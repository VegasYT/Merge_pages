/**
 * Этот файл содержит все Tailwind классы, используемые в компонентах редактора блоков.
 * Он нужен чтобы Tailwind JIT компилятор увидел эти классы при сканировании файлов.
 *
 * ПРИМЕЧАНИЕ: Градиенты НЕ включены здесь, так как они обрабатываются CDN Tailwind
 * для избежания конфликтов. CDN загружается динамически только на странице редактора.
 */

export const EDITOR_TAILWIND_CLASSES = `
	// Базовые классы для редактора (без градиентов)
	// Градиенты обрабатываются CDN Tailwind
`;
